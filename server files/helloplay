server {
    listen 80;
    server_name _;  # replace with your domain if you have one

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/ubuntu/sending/gunicorn.sock;
    }
}
